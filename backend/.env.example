PORT=5000
MONGODB_URI=mongodb://localhost:27017/peoplepay360
NODE_ENV=development
JWT_SECRET=peoplepay360_super_secret_jwt_key_2026

# Email / SMTP Configuration
# If omitted in development, Nodemailer automatically uses an Ethereal test inbox with clickable preview links.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM="PeoplePay360 <your-email@gmail.com>"
